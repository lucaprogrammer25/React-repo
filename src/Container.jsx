const Container = ({children}) => {
    return (
        <div style={{
            backgroundColor: "white",
            border:"1px solid red"
        }}>{children}</div>
    )
}

export default Container;